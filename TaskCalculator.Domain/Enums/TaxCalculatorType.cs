namespace TaskCalculator.Domain.Enums
{
    // Enum representing the available tax calculator implementations
    public enum TaxCalculatorType
    {
        Unspecified = 0,
        Zero = 1,
        Progressive = 2
    }
}
